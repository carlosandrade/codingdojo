function soma_romana(num1,num2) 
  local soma = "II"
  
  local valores = { I = 1, 
                    V = 5 }
--  if (valores[num1] and valores[num2]) then
--    soma = valores[num1] + valores[num2]
--  end
 
  return soma
end
